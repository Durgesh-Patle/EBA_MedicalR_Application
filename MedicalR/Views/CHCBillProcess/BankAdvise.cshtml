@model IEnumerable<MedicalR.Models.CHC.CHCBillProcess>
@{
    ViewBag.Title = "BankAdvise";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="container-fluid">
    <div class="row">
        <div class="col-md-12 p-0">
            <ul class="nav">
                @*<li class="nav-item"> <a class="nav-link text-dark" href=""><strong>CHC Bill Process / Office Note / Bank Advise Note – CHC Bill Process</strong></a> </li>*@
                <!--<li class="nav-item"> <a class="nav-link" href="update-batch-details.html">Update Batch</a> </li>-->
            </ul>
            <hr class="m-0">

        </div>

        <article class="col-md-12">
            <div class="pt-3">
            </div>
            <div class="pt-3">
            </div>
            <div class="pt-3">
            </div>

            <table id="dtBasicExample" class="table table-hover table-bordered" cellspacing="0" width="100%">
                <thead>
                    <tr>


                        <th class="th-sm">Name of CHC Center</th>
                        <th class="th-sm">Amount</th>
                        <th class="th-sm">Click for Bank Advise</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>



                            <td>@item.hosname</td>
                            <td hidden id="chc_centerid">@item.chc_centerid</td>
                            <td>@item.amount</td>
                            <td><span style="cursor:pointer" onclick="openBar()"></span> <a href="/CHCBillProcess/LetterForBank">Click for Bank Advise</a></td>
                        </tr>
                    }


                </tbody>
            </table>
            @if (Model.Count() == 0)
            {
                <div class="no-data-found">No Data Found</div>
            }
            @* </div> *@

        </article>
    </div>
</div>

<script>
    $(document).ready(function () {
        $('#dtBasicExample').DataTable();
        $('.dataTables_length').addClass('bs-select');


    });


</script>
